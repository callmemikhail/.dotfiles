--[nvim virables]--
----[vim conf]----
    g = vim.g
    o = vim.o
    fn = vim.fn
    cmd = vim.cmd
    opt = vim.opt
    keymap = vim.keymap
    optl = vim.opt_local
    map = vim.api.nvim_set_keymap 
